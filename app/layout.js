import './globals.css';

export const metadata = {
  title: 'Disponibilidad Presupuestal - HGZ No. 02',
};

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  );
}
